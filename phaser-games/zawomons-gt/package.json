{
  "name": "zawomons-gt",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npm run copy-to-public",
    "copy-to-public": "node scripts/copy-build.js"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.1"
  },
  "dependencies": {
    "phaser": "^3.70.0"
  }
}
